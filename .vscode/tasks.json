{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Format C++ Files",
            "type": "shell",
            "command": "find . -name '*.cpp' -o -name '*.h' | xargs clang-format -i",
            "options": {
                "cwd": "${workspaceFolder}/src"
            }
        }
    ]
}